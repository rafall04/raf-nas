# API bind ke localhost saja — hanya reverse proxy yang menghadap LAN.
API_HOST=127.0.0.1
API_PORT=4000

# PostgreSQL (dipakai mulai milestone DB)
DATABASE_URL=postgresql://rafnas:CHANGE_ME@127.0.0.1:5432/rafnas?schema=public
